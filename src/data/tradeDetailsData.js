import axios from 'axios';
import apiKeys from './apiKeys';

const URL = apiKeys.firebaseKeys.databaseURL;

const closeTrade = () => new Promise((resolve, reject) => {

});

export default {
  closeTrade,
};
